This application showcases a simple table app with data.

User can either add value or percentage to see the updated values.

To run the application in local follow the below steps: 
1. Clone the app using git clone https://github.com/NivedhaT20/react-table-application.git

2. Do npm install

3. Run npm run dev